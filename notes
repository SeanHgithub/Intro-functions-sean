import turtle
from turtle import *
t = Turtle()

#turtle.done
#define the function name it then pass arguments/parameters.

def rectangle(width, height):
    for i in range(2):
        t.forward(width)
        t.left(90)
        t.forward(height)
        t.left(90)
    
rectangle(100,125)
turtle.done()